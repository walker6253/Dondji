# 叮咚鸡使用手册

本手册分四章：

1. **第一章 系统使用手册**：刷好叮咚鸡固件后的日常操作（收音机、扫描、存台、命名、频谱、工具箱等）
2. **第二章 固件说明与刷机**：网站刷机/校准/配置、存储地址与技术说明
3. **第三章 常见问题与其它**：FAQ、联系方式
4. **第四章 授权与品牌**：商业使用说明、品牌使用政策

> 仅适配泉盛 **UV-K1**，以及 **UV-K5 / UV-K6 的 V3**。K5/K6 请确认机身标签有 **V3** 字样（K1 无 V3 之分）。

---

# 第一章 系统使用手册

欢迎使用 **叮咚鸡（Dondji）**。本章说明开机后的基本操作。刷机、校准、配置备份请见第二章。

## 1.1 基本操作

### 开关机与音量

- 旋钮开机 / 关机；旋转调节音量。
- 机身 **`#` 键即 F 键**（短按进入 F 组合键，长按锁键盘）。

### 打开菜单

1. 短按 **MENU** 进入菜单。
2. **↑/↓** 或数字键快速跳转菜单项；再按 **MENU** 进入子项。
3. **EXIT** 返回上一级或退出菜单。

### 频率模式（VFO）与信道模式（MR）

| 模式 | 说明 |
|------|------|
| **频率模式（VFO）** | 直接调谐频率；可改步进、调制等 |
| **信道模式（MR）** | 调用已保存的记忆信道 |

- 短按 **`3`** 或 **`F+3`** / 长按 **`3`**：在 VFO 与 MR 之间切换。
- **信道模式下修改亚音、功率、频率偏移等参数后，只是临时生效；必须再执行一次「存信道」，切换信道或开机后才会保留。** 详见 [1.4 如何保存信道](#14-如何保存信道)。
- 信道模式下 **`F+1`**：把当前信道复制到对应频段的 VFO，并切到频率模式（便于微调后再存回）。

### A / B 双频与接收模式

- **`F+2`** / 长按 **`2`**：切换 A/B（主信道模式下可能禁用）。
- 接收模式（单守 / 双守 / 跨段等）在菜单中设置；单守与双守界面示意见下图。

**单守页面：**

![单守页面示意](images/main_only_s.png)

**双守页面：**

![双守页面示意](images/AB_s.png)

### 常用按键（主界面）

| 按键 | 功能 |
|------|------|
| `长按1` 或 `F+1` | VFO 下：切换频段起始频率；MR 下：`F+1` 将信道复制到 VFO |
| `长按2` 或 `F+2` | 切换 A/B 信道（仅主信道模式下可能禁用） |
| `长按3` 或 `F+3` | 切换信道模式 / 频率模式 |
| `长按4` 或 `F+4` | 一键扫频 |
| `F+5` | 进入频谱 |
| `长按5` | 双守 + 双 VFO 时：范围扫描（见 1.3） |
| `长按6` 或 `F+6` | 切换功率 |
| `F+7` | 打开**工具箱**（电子木鱼 / CW），不是游戏 |
| `长按8` | 倒频（收发频率互换） |
| `F+8` | 切换背光模式 |
| `长按9` | 跳到「其它」→「按键即呼」中设置的信道 |
| `长按0` 或 `F+0` | 打开收音机 |
| 信道模式 `长按*` 或 `F+*` | 扫描当前扫描列表 |
| 频率模式 `长按*` | 频率向上扫描 |
| 频率模式 `F+*` | 扫描亚音 |
| `长按#`（F） | 锁键盘；再长按解锁 |
| `F+↑` / `F+↓` | 调节静噪（若固件启用） |
| `F+侧键` | 调节步进（若固件启用） |

### 菜单导航摘要

- **方向键**：切换菜单  
- **MENU**：确认  
- **EXIT**：退出  
- **数字键**：快速跳转  

---

## 1.2 如何打开与使用收音机

叮咚鸡使用机内 **BK1080** 做调频广播，通过**独立的收音机页面**收听（不是主界面调制里的 WFM）。

### 进入 / 退出

1. 主界面 **`长按0`** 或 **`F+0`** 进入收音机。
2. 再按 **`F+0`**，或短按 **EXIT**（无保存/删除提示时）退出回主界面。

### 频率模式与信道模式（收音机内）

- 收音机内短按 **`3`** 或 **`F+3`**：在 **频率（VFO）** 与 **信道（MR）** 之间切换。
- **频率模式**：用数字键输入频率，或 **↑/↓** 调谐。
- **信道模式**：在已保存的收音机预设台之间切换。

### 切换国家频段

- 收音机页面下 **短按 `1`** 或 **`F+1`** / **长按 `1`**：切换所在国家/地区频段（频段不对时先切换再搜台）。

### 保存 / 删除收音机预设

**频率模式下保存当前频率到预设列表：**

1. 调好频率后短按 **MENU**，出现「保存?」。
2. 用 **↑/↓** 选择预设槽位。
3. 再按 **MENU** 确认保存。

**信道（MR）模式下删除当前预设：**

1. 短按 **MENU**，出现「删除?」。
2. 再按 **MENU** 确认删除。

也可用网站「写频」相关能力管理部分配置；收音机选中频率/频道/MR/频段会写入配置区（见第二章）。

### 扫描电台

| 操作 | 效果 |
|------|------|
| **长按 `*`** 或 **`F+*`** | 清空预设列表后全频段搜索（A-SCAN），有信号的频率会存入列表 |
| 短按扫描相关操作 | 在已有列表上扫描（M-SCAN，界面有提示） |

停止扫描：短按 **EXIT** 或 **MENU**（视当前状态）。

### 其它按键

| 按键 | 功能 |
|------|------|
| `F+8` | 背光（按需） |
| `F+9` | 背光开关相关 |
| `F+0` | 退出收音机 |

### 注意

- 双守时：仅当**主信道**来业余信号时，才会打断并退出收音机（见 FAQ）。
- 收音机占用 BK1080；与双守轮询互斥策略见第三章 FAQ #12。

---

## 1.3 如何扫描

### 扫描记忆信道列表

1. 先给每个信道设置所属扫描列表：网页「写频」写入，或菜单 **「信道」→「信道列表」**。
2. **MENU** → **「其它」→「扫描列表」**，选择要扫的列表。
3. 主界面处于**信道模式**，**长按 `*`** 或 **`F+*`** 开始扫描。
4. 扫到有信号的信道后，按 **MENU** 停在该信道；无信号时 **MENU** / **EXIT** 均可退出扫描。

### 一键扫频与扫描亚音

1. **长按 `4`** 或 **`F+4`**：一键扫频。
2. **频率模式**下 **`F+*`**：开启扫描亚音（CTCSS/DCS）。

扫到结果后，可按提示 **MENU** →「保存?」→（信道模式时可选信道号）→ 再 **MENU** 确认，写入记忆信道。**EXIT** 取消。

### 范围扫描与频谱入口

1. 切换为**双守**模式（单守下范围扫描不可用），且“显示”中“频谱显示”选择“简单”。
2. A、B 均切换为**频率模式**。
3. A 输入起始频率，B 输入结束频率。
4. **长按 `5`**：进入范围扫描（长按 **EXIT** 退出）。
5. **`F+5`**：进入频谱页面（详见 1.6）。

### 扫描中排除信道

扫描暂停在某一信道上时，**长按 MENU** 可将该信道从当前扫描中排除（视固件提示）。

### 相关菜单

| 菜单 | 作用 |
|------|------|
| 扫描列表 | 选择默认列表 / 全部等 |
| 优先扫描、优先信道 | 优先信道 |
| 扫描恢复 | 遇信号后停止 / 载波 / 超时等 |
| 信道列表 | 信道是否加入某扫描列表 |

---

## 1.4 如何保存信道

把当前频率与参数写入记忆信道。

### 方法一：菜单「存信道」

1. 在主界面调好频率、亚音、功率、调制、偏移等。
2. 短按 **MENU** → **存信道（ChSave）** → 再按 **MENU**。
3. 用 **↑/↓** 或输入信道号，选择目标信道。
4. 按 **MENU** → 出现「确认?」后再按 **MENU** 完成保存。

删除：菜单 **删信道（ChDele）**，同样需确认。

也可用网站「写频」页批量导入信道。

### 方法二：信道模式下改参后必须再存

在**信道模式（MR）**下，通过菜单修改该信道的亚音、功率、带宽等时：

- 修改后**当时**可能已按新参数工作；
- **若不执行「存信道」**，一切换信道或开机，修改会丢失（视为临时更改）。

**正确做法：** 改完参数后，再走一遍 **存信道（ChSave）**，把当前参数写回该记忆信道。

### 方法三：扫描结果保存

一键扫频 / 亚音扫描结束后，按界面「保存?」流程写入信道（见 1.3）。

---

## 1.5 命名信道与拼音输入

### 进入命名

1. **MENU** → **命名信道（ChName）**。
2. 选择有效信道 → **MENU** 进入编辑。
3. 编辑完成后，短按 **MENU** 移动光标；到末尾再确认「确认?」保存。
4. 信道名最长约 **10 字节**（英文按字符；中文约 **5 个汉字**）。不含汉字时，双守界面可能把频率与信道名显示位置对调（见 FAQ #3）。

> 显示中文需要已刷字库，并在 **显示 → 显示语言** 中选中文。

### 输入模式

命名界面短按 **`#`（F）** 循环切换（中文界面默认常从拼音开始）：

| 模式 | 说明 |
|------|------|
| 拼音 | T9 拼音 → 选汉字 |
| 小写 / 大写 | 多击字母 |
| 数字 | 纯数字 |
| 符号 | 符号页 |

要点：

- 拼音：用 **2–9** 按九键输入音节数字；**↑/↓** 翻候选；**\*** 退格。
- 非拼音下长按 `#` 可插入 `#`；长按数字可强制输入该数字。
- 长按 `EXIT` 退出编辑模式。

#### 拼音按键对照（T9）

| 按键 | 字母 |
|------|------|
| **2** | A B C |
| **3** | D E F |
| **4** | G H I |
| **5** | J K L |
| **6** | M N O |
| **7** | P Q R S |
| **8** | T U V |
| **9** | W X Y Z |

示例：「山」可试 `7426`（shan）等，再从候选里选字。

---

## 1.6 频谱

主界面 **`F+5`** 进入频谱。

### 显示模式：简单 / 专业

进入频谱前，可在菜单中选择界面风格：

1. 短按 **MENU** → 找到 **频谱显示（SpDisp）** → 再按 **MENU**。
2. 用 **↑/↓** 选择后按 **MENU** 确认：

| 选项 | 说明 |
|------|------|
| **简单（Simple）** | 频谱波形偏下，频率游标两侧有辅助横线与刻度，读频更直观，适合日常扫频 |
| **专业（Expert）** | 信息更紧凑，上方留出更多参数/标记区域，适合需要同时看更多细节时使用 |

该设置会写入配置（SPI `0x00A148`），备份/恢复配置时一并保存。

![频谱页面示意](images/频谱.jpg)

**频谱页面按键：**

| 按键 | 功能 |
|------|------|
| **数字键** | |
| `0` | 切换调制 (FM/AM/USB) |
| `1` / `7` | 增加/减少扫描步进 |
| `2` / `8` | 增加/减少频率变化步进 |
| `3` / `9` | 增加/减少 dB 最大值范围 |
| `4` | 切换扫描点数 (128/64/32/16) |
| `5` | 频率输入模式 |
| `6` | 切换监听带宽 (25/12.5/6.25 kHz) |
| **功能键** | |
| `↑` / `↓` | 向上/向下调整频率 |
| `F` | 降低触发门限 |
| `*` | 提高触发门限 |
| `MENU` | （保留） |
| `EXIT` | 退出频谱 |
| **侧键** | |
| `SIDE1` | 黑名单当前频率 |
| `SIDE2` | 切换背光 |
| `PTT` | 进入 STILL 并调谐到峰值 |

---

## 1.7 工具箱（F+7）

主界面 **`F+7`** 打开 **工具箱**（不是打砖块游戏）：

| 项目 | 说明 |
|------|------|
| **电子木鱼** | 趣味敲击 |
| **cw** | 摩尔斯相关练习/功能 |

**EXIT** 退出工具箱。

---

## 1.8 MDC ID 与发射尾音

当前固件仍使用 **MDC1200**（Yan ID 替换方案见设计文档，**尚未实装**）。

1. **MENU** → **其它** → **发射尾音（Roger）**：可选 **关 / ROGER / MDC**。
2. 选 **MDC** 时，可在 **MDC ID** 菜单编辑 4 位十六进制单元 ID（为0000时，默认为未填写，不发送）。
3. 对方发 MDC 且本机 Roger 为 MDC 时，主界面可弹出 **MDC ID** 接收提示。

配置备份中 MDC ID 位于 SPI `0x00A172`（2 字节），见第二章地址表。

---

## 1.9 使用注意

- 发射/接收时电压表可能随电流跳动，属正常现象（见 FAQ #13）。
- 写频、校准务必使用本站或已验证工具；地址布局与其它固件不通用。
- 刷机步骤与校准备份见第二章。

---

# 第二章 固件说明与刷机

欢迎使用 **叮咚鸡刷机网站**。本章说明浏览器内备份校准、刷固件、字库、配置、写频与开机图，以及存储地址等技术细节。

## 2.1 准备工作与推荐顺序

1. 使用 **Chrome** 或 **Edge**（需支持 Web Serial）。
2. 原厂编程线连接手台与电脑。
3. **首次刷第三方固件前**，务必在**原厂固件、正常开机使用界面**下先 **备份校准**。

**推荐顺序：**

1. 备份校准（原厂、正常开机）  
2. 刷固件（BOOT 模式：关机后按住 **PTT** 再开机）  
3. 刷字库（正常开机；中文菜单/信道名需要）  
4. 校验 / 恢复校准（正常开机）  
5. 写频 / 开机图片 / 备份配置（按需）  

各步骤以网站对应页签为准。

## 2.2 备份校准

校准数据包含 RSSI、电池电压、VOX 等硬件参数，对正常工作至关重要。

**操作步骤：**

1. 设备正常开机进入使用界面（不要进 BOOT）
2. 连接 USB
3. 网页选择「备份校准」
4. 点击「导出校准数据」
5. 下载 `calibration.dat`（512 字节）

**技术实现：**

- 通过设备信息请求自动识别固件版本并选择校准区地址  
- **校准区 EEPROM 逻辑地址：**
  - 固件 **v5.0.0 及以上**：`0xB000`
  - 固件 **v5.0.0 以下**：`0x1E00`
- 每次读 16 字节，共 512 字节  
- 物理落盘在 SPI Flash `0x010000`–`0x0101FF`（见 2.5）

## 2.3 恢复校准

**操作步骤：**

1. 正常开机进入使用界面  
2. 连接 USB →「恢复校准」  
3. 选择此前的 `calibration.dat`（必须 512 字节）  
4. 点击恢复；完成后设备自动重启  

**重要提示：**

- ⚠️ **仅支持本站和 UVTools2 的备份文件**  
- ⚠️ **恢复前必须正常开机**，不能在刷机 BOOT 下操作  
- 写入完成后自动重启  

**技术实现：** 与备份相同的版本识别与地址；每次写 16 字节；完成后发重启命令。

## 2.4 校准地址与数据结构

**存储位置：**

| 存储介质 | 地址范围 | 用途 | 大小 |
|---------|---------|------|------|
| EEPROM 逻辑 | `0x1E00`（v4+） | 校准数据 | 512B |
| EEPROM 逻辑 | `0xB000`（v5+） | 校准数据 | 512B |
| SPI Flash | `0x010000` – `0x010200` | 校准数据备份区 | 512B |

**地址变更原因：** v5.0.0 起校准区从 `0x1E00` 迁到 `0xB000`；网页工具自动识别版本。两套逻辑地址均映射到同一 SPI 区域。

**校准数据结构（相对校准区偏移）：**

- `+0xC0` ~ `+0xD0`：RSSI  
- `+0x140` ~ `+0x14C`：电池电压  
- `+0x150` ~ `+0x160`：VOX 灵敏度  
- `+0x168` ~ `+0x178`：VOX 阈值  
- `+0x188` ~ `+0x198`：晶振、音量增益等  

**兼容性：** 不同网站格式可能不兼容；刷新固件前先备份；校准损坏可表现为电量乱跳、信号不准、花屏等。

## 2.5 备份与恢复配置

配置数据为菜单等设置项，便于恢复出厂后快速还原个性化配置。**不含信道频率/名称、校准、字库。**

### 使用场景

1. 网页「备份配置」导出  
2. 需要时「恢复配置」写回  

### 备份配置

1. 正常开机 → USB →「备份配置」→「导出配置数据」  
2. 得到 `config_backup.dat`（512 字节）  

**技术：** SPI 物理地址 `0x00A000` 起，大小 512 字节（`0x200`）；按块读取。

### 恢复配置

1. 正常开机 →「恢复配置」→ 选择备份文件 → 写入 → 自动重启  

### 备份内容详解

| 地址范围 | 内容 |
|---------|------|
| `0x00A000` (8B) | 音频设置(FM/AM)、静噪、发射超时、按键锁/MENU锁、VOX、麦克风灵敏度 |
| `0x00A008` (8B) | 背光最大/最小、信道显示模式、跨段、省电、双守候、背光时间、尾音消除、当前状态/列表 |
| `0x00A010` (16B) | 当前信道(A/B)、MR(A/B)、频率信道(A/B)、NOAA |
| `0x00A020` (4B) | FM 收音机：选中频率、选中频道、是否 MR、频段 |
| `0x00A028` (40B) | FM 频道列表 |
| `0x00A0A8` (8B) | 蜂鸣、侧键动作、扫描恢复、自动锁键、开机显示模式 |
| `0x00A0B0` (8B) | 开机密码 |
| `0x00A0B8` (8B) | 语音提示、dBm 校正表 |
| `0x00A0C0` (8B) | 报警模式、Roger、尾音时间、发射 VFO、电池类型 |
| `0x00A0D0` (8B) | DTMF 侧音、分隔码、组呼码、解码响应、自动复位、预加载等 |
| `0x00A0D8` (8B) | DTMF 码持续/间隔、允许远程 Kill |
| `0x00A0F8` (48B) | DTMF ANI/KILL/REVIVE/上行/下行码 |
| `0x00A130` (8B) | 扫描列表默认、使能、优先信道、CHAN_1_CALL |
| `0x00A138` (16B) | 自定义 AES 密钥 |
| `0x00A150` (8B) | F-LOCK、350TX/200TX/500TX、350EN、加密、电池显示、背光 TX/RX |
| `0x00A158` (8B) | 显示设置(SET_INV 等)、S0/S9 |
| `0x00A160` (16B) | 版本字符串 |
| `0x00A170` (2B) | 界面语言、启动提示 |
| `0x00A0B9` (7B) | dBm 校正表 |
| `0x00A0C8` (32B) | 开机 Logo 自定义文本 |

**不包含：**

- **信道数据**（频率、名称、属性）— `0x000000`–`0x008000`  
- **校准数据** — `0x010000` 区域  
- **中文字库** — `0x024000` 区域  

## 2.6 SPI Flash 存储地址映射

用户数据主要在 **PY25Q16 SPI Flash（2 MB，`0x000000`–`0x1FFFFF`）**。MCU 片内 Flash（`0x08002800` 起）仅放程序。

网页写频与固件多直接用 **SPI 物理地址**；旧工具的 **EEPROM 逻辑地址** 由 `eeprom_compat` 映射到 SPI。

### 存储介质概览

| 介质 | 地址范围 | 容量 | 用途 |
|------|----------|------|------|
| SPI Flash | `0x000000` – `0x1FFFFF` | 2 MB | 信道、设置、校准、字库、语音、Logo |
| MCU 片内 Flash | `0x08002800` – `0x0801FFFF` | 118 KB | 固件（Bootloader 在 `0x08000000`） |
| SRAM | `0x20000000` – `0x20003FFF` | 16 KB | 运行时 RAM |

### 地址总览

| SPI 物理地址范围 | 用途 | 大小 |
|------------------|------|------|
| `0x000000` – `0x003FFF` | MR 信道频率 | 16 KB |
| `0x004000` – `0x007FFF` | MR 信道名称 | 16 KB |
| `0x008000` – `0x00886D` | 信道属性 + 扫描列表名 | ~2.1 KB |
| `0x009000` – `0x0090D5` | VFO 频率数据 | ~214 B |
| `0x00A000` – `0x00A175` | 设置/配置 | ~373 B（备份区 512 B） |
| `0x010000` – `0x0101FF` | 校准数据 | 512 B |
| `0x020000` – `0x023FFF` | 旧版中文信道名（迁移后可擦） | 16 KB |
| `0x024000` – `0x056236` | 中文字库 + 拼音索引 | ~200 KB |
| `0x14C000` – `0x14CFFF` | 语音索引表 | ~4 KB |
| `0x14D000` – ~`0x165000` | 语音音频数据 | ~1.6 MB |
| `0x1FF000` – `0x1FF407` | 开机 Logo | 1032 B |

### 信道与 VFO

| SPI 物理地址 | 大小 | 内容 | 计算公式 |
|-------------|------|------|----------|
| `0x000000` – `0x000FFF` | 4 KB | MR 频率 Bank 0 | `0x000000 + channel × 16` |
| `0x001000` – `0x001FFF` | 4 KB | MR 频率 Bank 1 | |
| `0x002000` – `0x002FFF` | 4 KB | MR 频率 Bank 2 | |
| `0x003000` – `0x003FFF` | 4 KB | MR 频率 Bank 3 | |
| `0x004000` – `0x004FFF` | 4 KB | MR 信道名 Bank 0 | `0x004000 + channel × 16` |
| `0x005000` – `0x005FFF` | 4 KB | MR 信道名 Bank 1 | |
| `0x006000` – `0x006FFF` | 4 KB | MR 信道名 Bank 2 | |
| `0x007000` – `0x007FFF` | 4 KB | MR 信道名 Bank 3 | |
| `0x008000` – `0x00880D` | ~2 KB | 1024 信道属性（每信道 2 B） | `0x008000 + channel × 2` |
| `0x00880E` – `0x00886D` | 96 B | 扫描列表名称 | |
| `0x009000` – `0x0090D5` | ~214 B | 14 个 VFO × 16 B | |

### 设置项明细

配置备份区起始于 `0x00A000`，共 512 B。字段与 2.5 一致，此处补全：

| SPI 物理地址 | 大小 | 内容 |
|-------------|------|------|
| `0x00A000` | 8 B | 音频、静噪、超时、锁键、VOX、麦克风 |
| `0x00A008` | 8 B | 背光、信道显示、跨段、省电、双守、尾音等 |
| `0x00A010` | 16 B | 当前/MR/频率/NOAA 信道 |
| `0x00A020` | 4–8 B | FM 收音机状态 |
| `0x00A028` | 40 B | FM 频道列表 |
| `0x00A0A8` | 8 B+ | 蜂鸣、侧键、扫描恢复、自动锁键、开机显示 |
| `0x00A0B0` | 8 B | 开机密码 |
| `0x00A0B8` | 8 B | 语音、dBm 校正起始 |
| `0x00A0B9` | 7 B | dBm 校正表 |
| `0x00A0C0` | 8 B | 报警、Roger、尾音时间、发射 VFO、电池类型 |
| `0x00A0C8` | 16 B | Logo 自定义文本第 1 行 |
| `0x00A0D0` | 8 B | DTMF 相关 |
| `0x00A0D8` | 16 B | DTMF 时序 + Logo 文本第 2 行 |
| `0x00A0F8` | 48 B | DTMF ANI/KILL 等 |
| `0x00A130` | 8 B | 扫描列表 / 优先信道 |
| `0x00A138` | 16 B | AES 密钥 |
| `0x00A148` | 4 B | 频谱显示模式 |
| `0x00A150` | 8 B | F-LOCK、扩展 TX、加密、电池显示、背光 TX/RX |
| `0x00A158` | 8 B | 显示设置、S0/S9、构建选项 |
| `0x00A160` | 16 B | 版本字符串 |
| `0x00A170` | 1 B | 界面语言 |
| `0x00A172` | 2 B | **MDC1200 单元 ID** |
| `0x00A174` | 1 B | 启动提示 / 开机音效 |

### 校准数据（SPI）

| SPI 物理地址 | 大小 | 内容 |
|-------------|------|------|
| `0x010000` + level | 1 B × 6 | UHF 静噪阈值 |
| `0x010060` + level | 1 B × 6 | VHF 静噪阈值 |
| `0x0100C0` | 8 B | UHF RSSI |
| `0x0100C8` | 8 B | VHF RSSI |
| `0x0100D0` + band×16 + op×3 | 3 B | 发射功率校准 |
| `0x010140` | 12 B | 电池电压 |
| `0x010150` + level×2 | 2 B | VOX1 |
| `0x010168` + level×2 | 2 B | VOX0 |
| `0x010188` | 8 B | 晶振、音量/DAC 等 |

EEPROM `0x1E00`（v4.x）与 `0xB000`（v5.0.0+）均映射到 `0x010000`–`0x0101FF`。

### 中文字库与拼音索引

| SPI 物理地址 | 大小 | 内容 |
|-------------|------|------|
| `0x024000` | 162,384 B | 汉字点阵（6766 × 24 B） |
| `0x04BA10` | 27,064 B | Unicode → 字库索引 |
| `0x052388` | 15,918 B | 拼音索引（402 音节） |
| `0x056236` | 1 B | 字库版本号（当前 = 2） |

拼音记录：`[音节长度][ASCII 拼音][汉字数量][Unicode×2…]`

旧版中文信道名：`0x020000`–`0x023FFF`（与字库不重叠）。

### 语音提示与开机 Logo

| SPI 物理地址 | 大小 | 内容 |
|-------------|------|------|
| `0x14C000` | 8 B × N | 中文语音索引 |
| `0x14C800` | 8 B × N | 英文语音索引 |
| `0x14D000` + Offset | 可变 | 语音数据池 |
| `0x1FF000` | 8 B | Logo 头（魔数 `"DOND"`） |
| `0x1FF008` | 1024 B | Logo 位图 |

### EEPROM 逻辑地址映射

| EEPROM 逻辑地址 | SPI 物理地址 | 大小 | 内容 |
|----------------|-------------|------|------|
| `0x000000` – `0x003FFF` | 同左 | 16 KB | MR 频率 |
| `0x004000` – `0x007FFF` | 同左 | 16 KB | MR 名称 |
| `0x008000` – `0x00886D` | 同左 | ~2.1 KB | 属性 + 列表名 |
| `0x009000` – `0x0090D5` | 同左 | ~214 B | VFO |
| `0x00A000` – `0x00A170` | 同左 | 368 B | 设置 |
| `0x001E00` – `0x001FFF` | `0x010000` – `0x0101FF` | 512 B | 校准（v4.x） |
| `0x00B000` – `0x00B1FF` | `0x010000` – `0x0101FF` | 512 B | 校准（v5.0.0+） |

**未映射 / 特殊项：**

| 地址 | 状态 | 说明 |
|------|------|------|
| EEPROM `0x1C00` + index×16 | 未映射 | DTMF 联系人（读为 `0xFF`，未持久化） |
| `0x0F30` – `0x0F40` | 受保护 | USB 写入禁止覆盖（重载相关） |
| `0x0E98` – `0x0EA0` | 受保护 | USB 写入禁止覆盖（密码相关） |

## 2.7 开机画面与自定义图片

1. 菜单 **「显示」→「开机画面」**。  
2. 选项说明：  
   - **关闭**：无开机动画  
   - **默认**：大猩猩 / 电压 / 声音等；**仅在「默认」时，「开机提示 / 开机音效」类选项才生效**  
   - **自定义**：在网站上传自定义图片后显示  

## 2.8 刷机相关 FAQ

（完整 FAQ 见第三章；此处抽出刷机向问答。）

1. **刚刷入显示英文？**  
   刷对应字库，并在 **显示 → 显示语言** 选中文。

2. **变砖？**  
   是否刷了非本机固件、用了其它写频/改校准工具？地址布局因固件而异，写频软件不通用。务必按本站步骤操作。

3. **别人字库正常我缺字？**  
   字库与固件版本不一致，或浏览器缓存了旧字库文件——清理缓存后重刷。

4. **刷回原厂要密码？**  
   菜单 **「其它」→「恢复出厂」→「原厂」(OEM)** → 确认 → 见「请关机」后关机 → 用官方 CPS 刷原厂固件。该选项会擦除含 AES 写频密钥的区域，避免 CPS 要密码。详情见网站应急工具箱同名教程。建议加微信群求助（网站右侧悬浮入口）。

5. **雪花屏 / 电量乱跳 / 电压不准？**  
   校准异常：恢复原厂校准；若未备份，可用网页提供的原厂校准。

---

# 第三章 常见问题与其它

## 3.1 常见问题

1. **「信道」→「功率」里显示「用 xxx」是什么意思？**  
   答：用户在 **「其它」→「设置功率」** 中自定义的功率档，此处回显。

2. **刚刷入固件显示英文？**  
   答：需刷入对应字库，并在 **显示 → 显示语言** 选择中文。

3. **双信道时有时下方显示信道名、有时显示频率？**  
   答：设计如此。信道名不含汉字时，下方显示频率、左上角显示信道名。信道名最多约 5 个汉字（约 15 个英文字母），放不下时会互换显示位置。

4. **变砖了？**  
   答：是否刷了不属于你机器的固件？是否用了其它写频工具或乱改校准？是否严格按网页刷机步骤执行？  
   > 各固件内存地址用法不同，写频软件不通用。未经验证的软件导致变砖自负。

5. **别人字库正常，我缺某些字？**  
   答：字库与固件版本不一致，或浏览器缓存旧文件。清理缓存后重刷匹配版本。

6. **收音机频段不对？**  
   答：在收音机页面用 **`1` / 长按 `1` / `F+1`** 切换国家频段。详见 [1.2](#12-如何打开与使用收音机)。

7. **机器是 K1，系统信息却显示 K5？**  
   答：用户自定义。可同时按住 **PTT** 与下方紧挨侧键开机以解锁相关选项；电池类型选对应机型。界面通常只显示 K1 或 K5（K5/K6 本质同芯，外壳不同）。

8. **改了亚音等参数，切换信道或开机就没了？**  
   答：**信道模式下修改参数后，必须再执行「存信道」**，否则只是临时更改。详见 [1.4](#14-如何保存信道)。

9. **长按 # 锁键盘时连 PTT 也锁了？**  
   答：**「其它」→「锁键范围」** 可选只锁键盘或连同 PTT。

10. **刷回原厂要密码？**  
    答：用 **「其它」→「恢复出厂」→「原厂」(OEM)**，确认后见「请关机」再关机，用官方 CPS 刷原厂。详见网站应急工具箱教程。建议加微信群（网站右侧悬浮）。

11. **雪花屏 / 电量飘忽 / 电压不对？**  
    答：校准问题，恢复原厂校准；未备份可用网页原厂校准。

12. **收音机时业余信道来信号，有时打断退出有时不？**  
    答：双守下仅当**主信道**有信号才打断退出收音机。BK4819 不能真双守，靠轮询；收音机模式下若持续轮询会损害收音质量并更耗电。

13. **为什么看起来特别耗电、电量肉眼可见变化？**  
    答：收发时电流变化导致电压表跳动，类似老式电动车拧油门电量瞬降。属正常现象。部分其它固件发射时不刷新电量，看起来更“稳”。

14. **解锁全部频段能用哪些？受硬件限制吗？**  
    答：受 **BK4819** 限制：Band1 约 18–630 MHz，Band2 约 84–1300 MHz；**630–840 MHz 为硬件死区**，固件无法绕过。

    **解锁全部（全部解锁 / F_LOCK_NONE）大致可用范围：**

    | 模式 | 接收（RX） | 发射（TX） |
    |------|-----------|-----------|
    | 标准模式 | 50 MHz ~ 600 MHz | 50 MHz ~ 600 MHz |
    | 宽带接收模式 | 18 MHz ~ 1300 MHz（排除死区） | 18 MHz ~ 600 MHz + 470 MHz ~ 1300 MHz |

    **菜单「锁定频段」常见选项（与当前固件一致，含编译开关项）：**

    | 模式 | 说明（发射相关） |
    |------|------------------|
    | 默认 | 137–174 / 400–470 |
    | FCC 业余 | 144–148 / 420–450 |
    | CA 业余 | 144–148 / 430–450（若编译启用） |
    | CE 业余 | 144–146 / 430–440 |
    | GB 业余 | 144–148 / 430–440 |
    | 137–174 / 400–430 | 分区表 |
    | 137–174 / 400–438 | 分区表 |
    | PMR 446 | 若编译启用 |
    | GMRS/FRS/MURS | 若编译启用 |
    | 全部禁用 | 禁发 |
    | 全部解锁 | 需在菜单中连续选择约 **10 次** 才激活（防误触） |

    **其它：**

    - 约 280 MHz 为 VHF/UHF 分界，影响滤波与功放增益  
    - 宽带接收可听短波到 1.3 GHz，发射仍受频段表限制  

## 3.2 联系方式

- 抖音：小闫连不上  
- B站：小闫同学啊  
- 小红书：小闫同学  
- 微信视频号：小闫连不上  

## 3.3 其它

BD1AHN 开发。其它固件合集：https://www.yuque.com/yanyuliang/radio/wpiyfs070wcfr55s?singleDoc#  

山竹固件网站：https://ethanyan6.github.io/Mangosteen/  

---

# 第四章 授权与品牌

## 4.1 叮咚鸡（Dondji）商业使用说明

### 1. 项目开源协议

叮咚鸡（Dondji）固件源代码采用 Apache License 2.0 开源协议。

项目维护者：

**BD1AHN**

Apache License 2.0 授予用户合法获取、使用、修改和分发本项目源代码的权利。

---

### 2. 个人使用

个人无线电爱好者、开发者可以自由：

- 下载源码；
- 学习研究；
- 编译固件；
- 修改代码；
- 为个人设备刷写；
- 用于个人测试。

个人非商业用途不受额外限制。

---

### 3. 商业使用说明

Apache License 2.0 授权的是源代码使用权。

以下内容不属于 Apache License 2.0 授权范围：

- 叮咚鸡（Dondji）名称；
- Dondji Logo；
- 项目品牌；
- 官方网站；
- 官方刷机入口；
- 官方发布渠道；
- BD1AHN 官方维护身份；
- 官方合作关系。

---

### 4. 商业产品合作

任何单位、厂商或商业组织，如计划：

- 将 Dondji 固件预装到硬件产品并销售；
- 基于 Dondji 开发商业设备；
- 使用 Dondji 名称进行产品宣传；
- 制作联名硬件产品；
- 宣称获得 BD1AHN 官方支持或认证；

需要提前联系：

**BD1AHN**

并获得书面商业授权。

---

### 5. 未授权商业行为

未经 BD1AHN 授权，不得：

- 使用“叮咚鸡（Dondji）”作为商业产品名称；
- 使用 Dondji Logo；
- 宣称产品为 Dondji 官方版本；
- 暗示与 BD1AHN 存在官方合作关系；
- 冒充官方刷机网站；
- 删除项目版权声明。

---

### 6. 商业合作范围

商业授权可能包括：

- 固件预装授权；
- 联名硬件合作；
- 品牌授权；
- 官方认证；
- 技术支持；
- 定制开发；
- 商业发行支持。

具体合作方式由 BD1AHN 与合作方协商。

---

### 7. 官方说明

本文件不会限制 Apache License 2.0 已授予的源码使用权。

本文件用于明确：

- Dondji 品牌权益；
- 官方项目身份；
- 官方网站归属；
- 商业合作关系。

## 4.2 叮咚鸡（Dondji）品牌使用政策

### 品牌声明

“叮咚鸡（Dondji）”是由 BD1AHN 创建并维护的开源固件项目名称。

以下内容属于项目品牌资产：

- 叮咚鸡（Dondji）名称；
- Dondji Logo；
- 项目视觉元素；
- 官方网站；
- 官方刷机页面；
- 官方发布渠道；
- 官方认证标识。

---

### Apache License 2.0 说明

叮咚鸡（Dondji）源码采用 Apache License 2.0。

Apache License 2.0 仅授权源码。

该许可证不授予任何人：

- 使用 Dondji 品牌名称；
- 使用 Dondji Logo；
- 宣称官方身份；
- 宣称官方合作。

---

### 官方网站

叮咚鸡（Dondji）官方刷机网站：

https://ethanyan6.github.io/Dondji/

未经 BD1AHN 授权，任何第三方不得：

- 创建仿冒官方网站；
- 冒充官方刷机入口；
- 使用类似名称误导用户。

---

### 商业使用

未经 BD1AHN 书面许可，不得：

- 将 Dondji 用作商业产品品牌；
- 使用 Logo 销售产品；
- 宣称官方认证；
- 宣称官方合作。

---

### 合理使用

以下属于合理使用：

- 技术讨论；
- 项目引用；
- 学习研究；
- 开源贡献。

---

### 商业授权

商业合作请联系：

项目维护者：

BD1AHN

项目：

叮咚鸡（Dondji）
